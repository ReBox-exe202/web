# Public (exposed to client-side) variables must start with NEXT_PUBLIC_

# URL of the API that the frontend will call (client-side readable)
NEXT_PUBLIC_API_URL=https://localhost:7068/api/v1

# Public application URL used for QR generation and links
NEXT_PUBLIC_APP_URL=https://your-domain.com

# PayOS configuration (server or client as appropriate)
RETURN_URL=https://your-domain.com/checkout/return
ELEMENT_ID=payos-element-id
CHECKOUT_URL=https://payos.example.com/checkout
